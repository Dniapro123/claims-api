package com.ulad.claims;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication

// Main application class for the Claims API. This class bootstraps the Spring Boot application.
public class ClaimsApiApplication {

  // Main method to start the Spring Boot application.
  public static void main(String[] args) {
    SpringApplication.run(ClaimsApiApplication.class, args);
  }
}
